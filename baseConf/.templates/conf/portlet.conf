mongo.db.host="127.0.0.1"
mongo.db.name="${projectName}"
// mongo.db.username=
// mongo.db.password=

hibernate {
	hbm2ddl.auto = "update"

	hikari {
		minimumPoolSize=20
		maximumPoolSize=100
		idleTimeout=30000
	}
}

${projectName} {
	instanceable = false

	display {
		// category = "category.hidden"
	}

	modes="VIEW,EDIT"

}